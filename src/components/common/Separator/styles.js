export const style = (alignSelf, borderBottomColor, borderBottomWidth, marginVertical,
  width) =>
  ({
    separator: {
      alignSelf,
      borderBottomColor,
      borderBottomWidth,
      marginVertical,
      width
    }
  });

export default style;
