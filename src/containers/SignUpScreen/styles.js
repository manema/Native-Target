import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  background: { flex: 1 },
  dataContainer: {
    alignSelf: 'center',
    flex: 1,
    width: '55%',
  }
});

export default styles;
